# PyTorch, CUDA and cuDNN are supplied by the base image. Do not add torch here:
# installing it again can duplicate or replace the CUDA runtime.

# Qwen3-Reranker runtime.
transformers==4.57.6
accelerate==1.14.0
safetensors==0.8.0

# Twin2Attr inference and configuration.
numpy>=2.2,<3
polars==1.43.2
orjson==3.11.9
loguru==0.7.3
hydra-core==1.3.5

# Optional Twin2Attr preprocessing enabled by solution.json.
joblib==1.5.3
pint==0.25.3
pymorphy3==2.0.6
